# Requirements for BCCC DDoS Training Container
# DistilBERT-based IDS training on BCCC Cloud DDoS 2024 dataset

# Core ML/DL libraries
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
accelerate>=0.20.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# ML utilities
scikit-learn>=1.3.0

# Optional: for better performance
# torch-audio  # Only if needed
# torchvision  # Only if needed

# Note: CUDA support is optional but recommended for faster training
# Install CUDA-enabled PyTorch if GPU is available:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
