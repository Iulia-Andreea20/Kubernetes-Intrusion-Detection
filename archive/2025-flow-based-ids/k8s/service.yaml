apiVersion: v1
kind: Service
metadata:
  name: llm-ids-inference
  namespace: ids-system
  labels:
    app: llm-ids-inference
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-ids-inference
