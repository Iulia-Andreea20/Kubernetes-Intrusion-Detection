apiVersion: v1
kind: Service
metadata:
  name: ids-service
  namespace: runtime-ids
  labels:
    app.kubernetes.io/name: ids-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ids-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
